This folder holds the MySQL Connector/J driver jar, which is **not included in
this repository** (it's a large third-party binary, and MySQL's own download
page always has the current version).

See the **Requirements** section of the main [README](../README.md) for the
exact download link and the filename this app expects
(`mysql-connector-j.jar`).
